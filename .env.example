# Copy these settings into a local .env or export them in your shell.
ANTHROPIC_API_KEY=your-api-key
MODEL_ID=your-model-id
# Optional API-compatible endpoint:
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# Optional bearer authentication for endpoints that support it:
# ANTHROPIC_AUTH_TOKEN=your-auth-token
# As in the Python lesson, BASE_URL disables AUTH_TOKEN, not API_KEY.
